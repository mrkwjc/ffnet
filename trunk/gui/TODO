###########
4. Needed tool for dumping network output
5. Start and stop animations outside training thread.
6. Input status should be moved to application status:
   -2   no network, no data
   -1   no network, data
    0   network, no data
    1   network, input data ok
    2   network, input and target data ok
    3   network, input wrong
    4   network, target wrong
    5   network, input and target wrong
###########

On Windows:
* redirecting of output do not work
* PySide is needed for Qt? Qt not working
* Config button of figure toolbar do not appear
* Starting training is slow, training is memory hungry, especially for nproc>1, to much pickling i think.
* Autoscroll not working also in wx
